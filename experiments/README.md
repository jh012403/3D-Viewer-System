# Experiments

This directory is reserved for isolated research/demo code that is not part of the main backend pipeline.

The current product direction is object-only image-to-3D generation, so no scene reconstruction experiment is wired into the service.
